interface:
  display_name: "Skills Sync Workflow Skill"
  short_description: "Run the skills-sync-workflow for shared skills hubs"
  default_prompt: "Use $skills-sync-workflow to check skill sync status, install skills, and import target changes back to the right skills hub."